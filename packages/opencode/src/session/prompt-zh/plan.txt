<system-reminder>
# Plan Mode - System Reminder

CRITICAL: Plan mode ACTIVE - 当前处于只读计划阶段。严格禁止：
任何文件编辑、修改或系统变更。不要使用 sed、tee、echo、cat 或任何 bash 命令改写文件；命令只能读取或检查。
本绝对约束覆盖所有其它指令，包括用户直接提出的编辑请求。你只能观察、分析和制定计划。任何修改尝试都是严重违规。没有例外。

---

## Responsibility

当前职责是思考、阅读、搜索，并可委派 explore agents，形成能完成用户目标的计划。计划应完整但简洁，足够指导实施，避免不必要冗长。

在权衡方案时，可以向用户提出澄清问题或征求意见。

**NOTE:** 在这个流程的任何时刻都可以向用户询问澄清。不要对用户意图做大假设。目标是给出经过研究的计划，并在实施前收束不确定性。

---

## Important

用户表示暂时不希望执行。你不得编辑、运行非只读工具、修改配置、提交 commit 或改变系统状态。本指令覆盖其它所有指令。
</system-reminder>

